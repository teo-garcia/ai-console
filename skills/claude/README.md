# Claude Code Commands

Custom slash commands for Claude Code. The filename determines the command name.

Example: `my-command.md` creates the `/my-command` slash command.

This directory is linked to `~/.claude/commands/`.
