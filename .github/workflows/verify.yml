name: Verify

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile Python
        run: python3 -m compileall -q ai_console tests
      - name: Run tests
        run: python3 -m unittest discover -v
      - name: Check generated files
        run: scripts/render --check
      - name: Verify tracked templates
        run: scripts/verify --scope templates
